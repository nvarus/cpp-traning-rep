#include <iostream>
int main()
{
    using namespace std;
    char ch;
    int count = 0;
    cout << "Введите символы\n";

    cin.get(ch);
    cout << ch;
    
    cin.get(ch);
    cout << ch;
    
    cin.get(ch);
    cout << ch;

    cin.get(ch);
    cout << ch;

    return 0;
}
